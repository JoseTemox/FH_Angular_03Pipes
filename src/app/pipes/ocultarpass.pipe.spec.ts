import { OcultarpassPipe } from './ocultarpass.pipe';

describe('OcultarpassPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarpassPipe();
    expect(pipe).toBeTruthy();
  });
});
